# Application
APP_NAME=Farda MCP
APP_VERSION=0.1.0
APP_ENV=local

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/farda_mcp

# Authentication & JWT
AUTH_SECRET=change-me-in-production
AUTH_ACCESS_TTL_MIN=60

# OIDC Configuration (for production)
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=

# CORS
# CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Secrets Management (Envelope Encryption)
SECRETS_MASTER_KEY=change-this-to-32-bytes-hex-or-base64
